export enum Role {
  STAFF = "staff",
  TRAINER = "trainer",
  ADMIN = "admin",
}

export enum AssignmentStatus {
  PENDING = "pending",
  SUBMITTED = "submitted",
  SIGNEDOFF = "signedoff",
  EXPIRED = "expired",
}
